{{#if username}}
  <div class="jumbotron d-flex justify-content-between">
    <h1>👽 {{this.username}}.</h1>
  </div>
{{else}}
  <div class="jumbotron d-flex justify-content-between">
    <h1>Acceso denegado.</h1>
  </div>
{{/if}}