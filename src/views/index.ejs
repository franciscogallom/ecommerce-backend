<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootswatch@4.5.2/dist/lux/bootstrap.min.css"
    />
    <title>Productos.</title>
  </head>
  <body class="bg-dark">
    <div class="w-50 m-auto">
      <h1 class="mt-2 text-light">Lista de productos.</h1>
      <br />

      <% if(products.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-dark">
          <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Foto</th>
          </tr>
          <% for(let i = 0; i < products.length; i++) { %>
          <tr>
            <td><%= products[i].title %></td>
            <td><%= products[i].price %></td>
            <td>
              <img
                width="50"
                src="<%= products[i].thumbnail %>"
                alt="not found"
              />
            </td>
          </tr>
          <% } %>
        </table>
      </div>
      <% } else { %>
      <h3 class="alert alert-warning">No hay productos en la lista.</h3>
      <% } %>

      <h2 class="mt-2 text-light">Agregar productos.</h2>
      <form action="/api/productos/guardar" method="POST">
        <div class="form-group">
          <label for="formGroupExampleInput" class="text-light">Nombre.</label>
          <input
            type="text"
            name="title"
            class="form-control"
            id="formGroupExampleInput"
            placeholder="Computadora"
            required
          />
        </div>
        <div class="form-group">
          <label for="formGroupExampleInput2" class="text-light">Precio.</label>
          <input
            type="text"
            name="price"
            class="form-control"
            id="formGroupExampleInput2"
            placeholder="150000"
            required
          />
        </div>
        <div class="form-group">
          <label for="formGroupExampleInput3" class="text-light"
            >Foto URL.</label
          >
          <input
            type="text"
            name="thumbnail"
            class="form-control"
            id="formGroupExampleInput3"
            placeholder="https://cdn3.iconfinder.com/data/icons/pc"
            required
          />
        </div>
        <button class="btn btn-secondary">Enviar</button>
      </form>
    </div>
  </body>
</html>
